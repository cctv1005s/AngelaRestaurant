var expect = require('chai').expect;
var co = require('co');
var model_order = require('../proxy/order.js');

describe('订单model的测试',function(){
    it('订单reserve方法测试',function (done) {
        co(function*(){
        });
    });
});